spring:
  application:
    name: seata-account-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: public
        group: SEATA_GROUP
    alibaba:
      seata:
        tx-service-group: fsp_tx_group
server:
  port: 2001


