server:
  port: 2002
spring:
  application:
    name: order-service

  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: public
        group: SEATA_GROUP
    alibaba:
      seata:
        tx-service-group: fsp_tx_group




